import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact Page",
    description: "Contact Page Describtion",
};

const ContactUs = () => {

    return (
        <div>
            Contact Page
        </div>
    )
}

export default ContactUs